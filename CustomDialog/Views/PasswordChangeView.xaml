<UserControl x:Class="CustomDialog.PasswordChangeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomDialog"
             mc:Ignorable="d" 
             d:DesignHeight="210" d:DesignWidth="350">
    <UserControl.Resources>
        <local:CommandParametersConverter x:Key="CommandParametersConverter"/>
    </UserControl.Resources>
    
    <Grid
        Background="{StaticResource LightBlueBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            Style="{StaticResource DefaultText}"
            Text="Пароль"/>

        <PasswordBox
            x:Name="first"
            Grid.Row="1"
            Style="{StaticResource MainPwdBox}"/>

        <TextBlock
            Grid.Row="2"
            Style="{StaticResource DefaultText}"
            Text="Подтверждение пароля"/>

        <PasswordBox
            x:Name="second"
            Grid.Row="3"
            Style="{StaticResource MainPwdBox}"/>

        <Grid
            Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button
                Style="{StaticResource CancelButton}"
                Content="Отмена"
                Command="{Binding CancelCommand}"/>

            <Button
                Grid.Column="1"
                Style="{StaticResource AcceptButton}"
                Content="Принять"
                Command="{Binding AcceptCommand}">
                <Button.CommandParameter>
                    <MultiBinding Converter="{StaticResource CommandParametersConverter}">
                        <Binding ElementName="first"/>
                        <Binding ElementName="second"/>
                    </MultiBinding>
                </Button.CommandParameter>
            </Button>
        </Grid>

    </Grid>
</UserControl>
