<UserControl x:Class="LogistHelper.UI.CustomControls.CustomPasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LogistHelper.UI.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
    
        <PasswordBox
            x:Name="pwdBox"
            Style="{Binding RelativeSource={RelativeSource AncestorType=local:CustomPasswordBox}, Path=PasswordBoxStyle}"
            Visibility="Visible"
            PasswordChanged="pwdBox_PasswordChanged"/>
    
        <TextBox
            x:Name="showedPwd"
            Style="{Binding RelativeSource={RelativeSource AncestorType=local:CustomPasswordBox}, Path=ShowedPasswordBoxStyle}"
            Visibility="Hidden"
            TextChanged="showedPwd_TextChanged"/>
    
        <ToggleButton
            x:Name="showToggle"
            Grid.Column="1"
            IsChecked="{Binding RelativeSource={RelativeSource AncestorType=local:CustomPasswordBox}, Path=IsPasswordShowed}"
            Style="{Binding RelativeSource={RelativeSource AncestorType=local:CustomPasswordBox}, Path=ShowButtonStyle}">
            <Image
                x:Name="showImage">
                <Image.Style>
                    <Style>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=local:CustomPasswordBox}, Path=IsPasswordShowed}" Value="True">
                                <Setter Property="Image.Source" Value="/UI/Icons/show_icon.png"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=local:CustomPasswordBox}, Path=IsPasswordShowed}" Value="False">
                                <Setter Property="Image.Source" Value="/UI/Icons/hide_icon.png"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>
        </ToggleButton>
    </Grid>
</UserControl>
