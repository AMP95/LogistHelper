<UserControl x:Class="LogistHelper.UI.CustomControls.WatermarkTextbox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LogistHelper.UI.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
        <TextBlock
            IsEnabled="False"
            Focusable="False"
            x:Name="watermark"
            Text="{Binding RelativeSource={RelativeSource AncestorType=local:WatermarkTextbox}, Path=Watermark}"
            TextAlignment="{Binding RelativeSource={RelativeSource AncestorType=local:WatermarkTextbox}, Path=TextBox.TextAlignment}"
            Style="{StaticResource DefaultText}"
            Foreground="{StaticResource SemiDarkBrush}"
            TextWrapping="NoWrap"
            Margin="12 0"/>

        <Grid
            x:Name="inputGrid">

        </Grid>
        
    </Grid>
</UserControl>
