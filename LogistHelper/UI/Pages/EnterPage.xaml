<UserControl x:Class="LogistHelper.UI.Pages.EnterPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LogistHelper.UI.Pages"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:models="clr-namespace:LogistHelper.Models"
             xmlns:converters="clr-namespace:LogistHelper.UI.Converters"
             xmlns:controls="clr-namespace:LogistHelper.UI.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid
            Grid.Row="1"
            Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>

                <TextBlock
                    Grid.Column="1"
                    Style="{StaticResource HeaderOneText}"
                    Text="Вход"/>

                <controls:Loader
                    Grid.Column="2"
                    Height="30"
                    HorizontalAlignment="Left"
                    IsActive="{Binding IsOnEnter}">
                    <controls:Loader.Style>
                        <Style>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsOnEnter}" Value="False">
                                    <Setter Property="controls:Loader.Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </controls:Loader.Style>
                </controls:Loader>
            </Grid>

            

            <TextBox
                Grid.Row="1"
                Style="{StaticResource MainLeftTextBox}"
                Text="{Binding Login}"/>

            <controls:CustomPasswordBox 
                Grid.Row="2"
                ShowButtonStyle="{StaticResource ActivePwdToggle}"
                KeyUp="passboxPasswordBox_KeyUp"
                Style="{StaticResource CustomPwdStyle}"
                x:Name="passboxPasswordBox"/>

            <Button
                Grid.Row="3"
                x:Name="enterButton"
                Style="{StaticResource NeutralButton}"
                Content="Вход"
                Command="{Binding EnterCommand}"
                CommandParameter="{Binding ElementName=passboxPasswordBox, Path=PasswordBox}"/>
        </Grid>
    </Grid>
</UserControl>
