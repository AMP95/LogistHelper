<UserControl x:Class="LogistHelper.UI.Pages.MenuPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LogistHelper.UI.Pages"
             xmlns:views="clr-namespace:LogistHelper.UI.Views"
             xmlns:pagesVM="clr-namespace:LogistHelper.ViewModels.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <views:NavigationBack
            HorizontalAlignment="Right"/>

        <ContentControl
            Grid.Row="1"
            Content="{Binding RelativeSource={RelativeSource AncestorType=local:MenuPage}, Path=DataContext}">
            <ContentControl.Resources>
                <DataTemplate
                    DataType="{x:Type pagesVM:MainMenuPageViewModel}">
                    <views:MainMenuView/>
                </DataTemplate>

                <DataTemplate
                    DataType="{x:Type pagesVM:DatabaseMenuPageViewModel}">
                    <views:DatabaseMenuView/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</UserControl>
