<UserControl x:Class="LogistHelper.UI.Views.BlockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LogistHelper.UI.Views"
             xmlns:controls="clr-namespace:LogistHelper.UI.CustomControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Style>
        <Style>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsBlock}" Value="True">
                    <Setter Property="UserControl.Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsBlock}" Value="False">
                    <Setter Property="UserControl.Visibility" Value="Hidden"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>

    <Grid
        Background="{StaticResource LightBlueBrush}">

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        

        <Grid
            Grid.Row="1"
            Grid.Column="1"
            Margin="10"
            Background="{StaticResource LightBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="45"/>
            </Grid.ColumnDefinitions>

            <TextBlock
                Style="{StaticResource HeaderOneText}"
                Foreground="{StaticResource NeutralOneBrush}"
                Text="{Binding BlockText}"/>

            <controls:Loader
                Grid.Column="1"
                IsInProcess="{Binding IsBlock, Mode=OneWay}"/>

        </Grid>

    </Grid>
</UserControl>
